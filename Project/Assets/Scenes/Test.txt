tree("Root")
	parallel
		sequence
			SetColor(1.0,0.0,0.0)
			Wait 1.0

			SetColor(0.0,1.0,0.0)
			Wait 1.0

			SetColor(0.0,0.0,1.0)
			Wait 1.0
		while
			not
				Fail
			sequence
				Move(true)
				Wait 1.0

				Move(false)
				Wait 1.0
